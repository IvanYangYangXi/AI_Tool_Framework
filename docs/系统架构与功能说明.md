<!-- 
  DCC工具管理框架 - 系统架构与功能说明
  版本: 2.0.0
  更新日期: 2026-02-16
-->

# 🏗️ DCC工具管理框架 - 系统架构与功能说明

> 本文档描述了框架的完整架构、功能模块和运行规则

---

## 📋 目录

1. [系统概述](#系统概述)
2. [架构流程图](#架构流程图)
3. [核心模块说明](#核心模块说明)
4. [自动化系统](#自动化系统)
5. [插件系统](#插件系统)
6. [触发器类型](#触发器类型)
7. [状态机规则](#状态机规则)
8. [数据流程](#数据流程)

---

## 系统概述

### 🎯 项目定位

DCC工具管理框架是一个**统一的工具插件管理平台**，支持：

| 功能 | 说明 |
|------|------|
| 🔌 **插件管理** | 发现、加载、卸载、执行各类DCC/UE工具插件 |
| 🤖 **自动化执行** | 定时、间隔、文件监控、任务链等多种触发方式 |
| 🔗 **DCC连接** | 与Maya、3ds Max、Blender、UE引擎实时通信 |
| 📊 **可视化界面** | 轻量级桌面GUI管理器 |
| 📁 **Git集成** | 版本控制、团队协作、自动更新 |

### 🖥️ 支持的DCC软件

```
┌─────────────────────────────────────────────────────────────┐
│                    DCC工具管理框架                          │
├─────────────────────────────────────────────────────────────┤
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────┐   │
│  │  Maya    │ │  3ds Max │ │ Blender  │ │ Unreal Engine│   │
│  │  :7001   │ │  :7002   │ │  :7003   │ │   :30010     │   │
│  │ (Socket) │ │ (Socket) │ │ (Socket) │ │   (HTTP)     │   │
│  └──────────┘ └──────────┘ └──────────┘ └──────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

---

## 架构流程图

### 🗺️ 系统整体架构

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           用户界面层 (GUI)                              │
│  ┌───────────────────────────────────────────────────────────────────┐  │
│  │  lightweight_manager.py - 轻量级工具管理器                        │  │
│  │  ├── 工具列表面板 (按DCC类型/自定义分组显示)                       │  │
│  │  ├── 工具详情面板 (参数配置、执行控制)                            │  │
│  │  ├── 日志输出面板 (实时执行日志)                                  │  │
│  │  └── 菜单栏 (Git管理、自动化、设置)                               │  │
│  └───────────────────────────────────────────────────────────────────┘  │
│                                   │                                     │
│                                   ▼                                     │
├─────────────────────────────────────────────────────────────────────────┤
│                           自动化调度层                                  │
│  ┌───────────────────────────────────────────────────────────────────┐  │
│  │  automation_manager.py - 自动化任务管理器                         │  │
│  │  ├── _scheduler_loop() - 后台调度循环 (1秒轮询)                   │  │
│  │  ├── create_task() - 创建自动化任务                               │  │
│  │  ├── _execute_task() - 执行任务并更新状态                         │  │
│  │  └── 文件监控 (watchdog) - 文件变化检测                           │  │
│  └───────────────────────────────────────────────────────────────────┘  │
│                                   │                                     │
│                                   ▼                                     │
├─────────────────────────────────────────────────────────────────────────┤
│                           核心执行层 (Core)                             │
│  ┌─────────────────┐ ┌─────────────────┐ ┌─────────────────────────┐   │
│  │ PluginManager   │ │ DynamicLoader   │ │ ConfigManager           │   │
│  │ - 插件发现      │ │ - 安全加载模块  │ │ - 解析config.json       │   │
│  │ - 插件注册      │ │ - 沙箱执行      │ │ - 验证配置格式          │   │
│  │ - 生命周期管理  │ │ - 超时控制      │ │ - 生成配置模板          │   │
│  └─────────────────┘ └─────────────────┘ └─────────────────────────┘   │
│                                   │                                     │
│                                   ▼                                     │
├─────────────────────────────────────────────────────────────────────────┤
│                           插件层 (Plugins)                              │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │ src/plugins/                                                     │    │
│  │ ├── dcc/          - DCC软件插件 (Maya/Max/Blender)              │    │
│  │ ├── ue/           - Unreal Engine插件                           │    │
│  │ ├── other/        - 通用工具 (非DCC特定)                        │    │
│  │ └── triggers/     - 触发器定义                                  │    │
│  └─────────────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────────────┘
```

### 🔄 工具执行流程

```
┌──────────┐    ┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│  用户    │───▶│  选择工具    │───▶│  配置参数    │───▶│  点击执行    │
│  操作    │    │  (GUI列表)   │    │  (详情面板)  │    │  (按钮)      │
└──────────┘    └──────────────┘    └──────────────┘    └──────────────┘
                                                               │
                     ┌─────────────────────────────────────────┘
                     ▼
            ┌─────────────────┐
            │  执行模式判断   │
            └────────┬────────┘
                     │
         ┌───────────┼───────────┐
         ▼           ▼           ▼
┌──────────────┐ ┌──────────┐ ┌──────────────┐
│ 在DCC中执行  │ │ 独立执行 │ │ 生成脚本    │
│ (Socket通信) │ │ (子进程) │ │ (复制代码)  │
└──────┬───────┘ └────┬─────┘ └──────────────┘
       │              │
       ▼              ▼
┌──────────────────────────────┐
│        返回执行结果          │
│  {"status": "success/error", │
│   "result": ...,             │
│   "output": "..."}           │
└──────────────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│     日志面板显示结果         │
└──────────────────────────────┘
```

---

## 核心模块说明

### 📦 模块依赖关系

```
                          ┌─────────────────────────┐
                          │   lightweight_manager   │
                          │       (GUI入口)         │
                          └───────────┬─────────────┘
                                      │
                    ┌─────────────────┼─────────────────┐
                    ▼                 ▼                 ▼
          ┌─────────────────┐ ┌─────────────┐ ┌─────────────────┐
          │ automation      │ │ trigger     │ │ automation      │
          │ _manager        │ │ _manager    │ │ _dialog         │
          └────────┬────────┘ └──────┬──────┘ └────────┬────────┘
                   │                 │                 │
                   ▼                 ▼                 ▼
          ┌─────────────────────────────────────────────────────┐
          │              trigger_config_widget                  │
          │          (触发器配置通用组件)                       │
          └─────────────────────────────────────────────────────┘
                                      │
                    ┌─────────────────┼─────────────────┐
                    ▼                 ▼                 ▼
          ┌─────────────────┐ ┌─────────────┐ ┌─────────────────┐
          │ plugin_manager  │ │ dynamic     │ │ config_manager  │
          │ (插件管理)      │ │ _loader     │ │ (配置管理)      │
          └─────────────────┘ └─────────────┘ └─────────────────┘
```

### 📁 核心文件说明

| 文件 | 职责 | 关键方法 |
|------|------|----------|
| `lightweight_manager.py` | GUI主界面 | `_execute_tool()`, `_connect_to_dcc()` |
| `automation_manager.py` | 自动化任务调度 | `_scheduler_loop()`, `_execute_task()` |
| `automation_dialog.py` | 任务管理对话框 | `_create_task_dialog()`, `_save_task()` |
| `trigger_config_widget.py` | 触发器配置UI | `get_trigger_config()`, `load_config()` |
| `trigger_manager.py` | 触发器类型注册 | `get_trigger_types()`, `validate_config()` |
| `plugin_manager.py` | 插件生命周期 | `discover_plugins()`, `load_plugin()` |
| `dynamic_loader.py` | 安全代码执行 | `load_module_safely()`, `execute_function_safely()` |
| `config_manager.py` | 配置文件解析 | `parse_sdd_config()`, `save_config()` |

---

## 自动化系统

### ⚙️ 自动化任务生命周期

```
┌──────────────────────────────────────────────────────────────────────────┐
│                         自动化任务生命周期                               │
├──────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│   ┌─────────┐     ┌─────────┐     ┌─────────┐     ┌─────────┐           │
│   │ 创建    │────▶│ 等待中  │────▶│ 运行中  │────▶│ 完成    │           │
│   │ (idle)  │     │(waiting)│     │(running)│     │(complet)│           │
│   └─────────┘     └────┬────┘     └────┬────┘     └─────────┘           │
│                        │               │                                 │
│                        │               │ 失败                            │
│                        │               ▼                                 │
│                   禁用 │          ┌─────────┐                            │
│                        ▼          │  错误   │                            │
│                   ┌─────────┐     │ (error) │                            │
│                   │ 已暂停  │     └─────────┘                            │
│                   │(paused) │                                            │
│                   └─────────┘                                            │
│                                                                          │
└──────────────────────────────────────────────────────────────────────────┘
```

### 📊 任务状态说明

| 状态 | 图标 | 说明 | 触发条件 |
|------|------|------|----------|
| `idle` | 💤 | 空闲 | 初始状态（很少见） |
| `waiting` | ⏳ | 等待中 | 任务已启用，等待触发 |
| `running` | ▶️ | 运行中 | 正在执行工具脚本 |
| `completed` | ✅ | 已完成 | 单次执行成功 |
| `paused` | ⏸️ | 已暂停 | 任务被禁用 |
| `error` | ❌ | 错误 | 执行失败 |

### 🔁 调度器工作原理

```
┌─────────────────────────────────────────────────────────────┐
│                  _scheduler_loop() 主循环                   │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   while self._running:                                      │
│       │                                                     │
│       ▼                                                     │
│   ┌─────────────────────────────────────┐                   │
│   │ 1. 获取当前时间 now = datetime.now()│                   │
│   └─────────────────┬───────────────────┘                   │
│                     ▼                                       │
│   ┌─────────────────────────────────────┐                   │
│   │ 2. 加锁扫描任务列表                 │                   │
│   │    with self._lock:                 │                   │
│   │      for task in tasks:             │                   │
│   │        if task.enabled and          │                   │
│   │           now >= task.next_run:     │                   │
│   │          tasks_to_execute.append()  │                   │
│   └─────────────────┬───────────────────┘                   │
│                     ▼                                       │
│   ┌─────────────────────────────────────┐                   │
│   │ 3. 释放锁后执行任务 (防止死锁)      │  ◀── 关键设计    │
│   │    for task in tasks_to_execute:    │                   │
│   │      self._execute_task(task)       │                   │
│   └─────────────────┬───────────────────┘                   │
│                     ▼                                       │
│   ┌─────────────────────────────────────┐                   │
│   │ 4. 等待1秒 time.sleep(1)            │                   │
│   └─────────────────┬───────────────────┘                   │
│                     │                                       │
│                     └──────────────────────▶ 循环继续       │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 插件系统

### 🔌 插件目录结构

```
src/plugins/
├── dcc/                          # DCC软件专用插件
│   ├── maya/                     # Maya插件
│   │   └── example_print_selection/
│   │       ├── config.json       # 插件配置
│   │       └── plugin.py         # 插件代码
│   ├── max/                      # 3ds Max插件
│   │   └── example_print_selection/
│   └── blender/                  # Blender插件
│       └── example_print_selection/
│
├── ue/                           # Unreal Engine插件
│   ├── example_print_selection/
│   ├── init_unreal.py            # UE初始化脚本
│   └── ue_script_listener.py     # UE脚本监听器
│
├── other/                        # 通用工具（非DCC特定）
│   └── clipboard_info/           # 剪贴板信息工具
│
└── triggers/                     # 触发器定义
    ├── scheduled_trigger.py      # 定时触发器
    ├── interval_trigger.py       # 间隔触发器
    ├── file_watch_trigger.py     # 文件监控触发器
    └── task_chain_trigger.py     # 任务链触发器
```

### 📝 插件配置文件规范 (config.json)

```json
{
  "plugin": {
    "name": "工具显示名称",
    "version": "1.0.0",
    "type": "dcc | ue_engine | other",
    "target_dcc": "maya | max | blender | unreal | other",
    "min_version": "2022",
    "max_version": "2025",
    "description": "工具功能描述",
    "author": "作者名",
    "entry_point": "plugin.py",
    "tags": ["modeling", "animation", "utility"]
  },
  "parameters": {
    "参数名": {
      "type": "string | number | boolean",
      "default": "默认值",
      "description": "参数说明"
    }
  }
}
```

### 🏷️ 分组标签

| 标签ID | 显示名称 | 说明 |
|--------|----------|------|
| `modeling` | 🧊 建模 | 模型相关工具 |
| `animation` | 🎬 动画 | 动画相关工具 |
| `rigging` | 🦴 绑定 | 骨骼绑定工具 |
| `io` | 📥 导入导出 | 导入/导出工具 |
| `utility` | 🔧 通用 | 通用工具 |
| *自定义* | *用户定义* | 通过"管理分组"功能添加 |

---

## 触发器类型

### ⏰ 触发器类型概览

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          触发器类型                                     │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐         │
│  │   ⏰ 定时触发    │  │   🔄 间隔触发    │  │   📁 文件监控    │         │
│  │   (Scheduled)   │  │   (Interval)    │  │  (File Watch)   │         │
│  │                 │  │                 │  │                 │         │
│  │ 每天09:00执行   │  │ 每5分钟执行一次 │  │ 文件变化时执行  │         │
│  │ 每周一10:30执行 │  │ 每30秒执行一次  │  │ 支持过滤器      │         │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘         │
│                                                                         │
│  ┌─────────────────┐  ┌─────────────────┐                              │
│  │   🔗 任务链      │  │   📦 手动触发    │                              │
│  │  (Task Chain)   │  │   (Manual)      │                              │
│  │                 │  │                 │                              │
│  │ 任务A完成后     │  │ 仅手动执行      │                              │
│  │ 自动触发任务B   │  │ 或API调用       │                              │
│  └─────────────────┘  └─────────────────┘                              │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 📋 触发器配置详情

#### 1️⃣ 定时触发器 (scheduled)

```json
{
  "type": "scheduled",
  "schedule_type": "daily | weekly | once",
  "time": "09:00",
  "weekday": 0,         // 0=周一 (仅weekly模式)
  "date": "2026-02-16"  // 仅once模式
}
```

#### 2️⃣ 间隔触发器 (interval)

```json
{
  "type": "interval",
  "interval_value": 5,
  "interval_unit": "seconds | minutes | hours"
}
```

#### 3️⃣ 文件监控触发器 (file_watch)

```json
{
  "type": "file_watch",
  "watch_path": "D:/Projects/Assets",
  "patterns": ["*.fbx", "*.obj"],
  "events": ["created", "modified", "deleted"]
}
```

#### 4️⃣ 任务链触发器 (task_chain)

```json
{
  "type": "task_chain",
  "predecessor_task_id": "task_001",
  "trigger_on": "success | failure | always"
}
```

---

## 状态机规则

### 📊 任务状态转换规则

```
┌─────────────────────────────────────────────────────────────────────────┐
│                      任务状态转换规则                                   │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌─────────┐                                                            │
│  │  创建   │                                                            │
│  │  任务   │                                                            │
│  └────┬────┘                                                            │
│       │                                                                 │
│       ▼                                                                 │
│  ┌─────────┐  enabled=True   ┌─────────┐  触发时间到  ┌─────────┐       │
│  │  idle   │───────────────▶│ waiting │─────────────▶│ running │       │
│  └─────────┘                 └────┬────┘              └────┬────┘       │
│       ▲                          │                        │             │
│       │                          │ enabled=False          │             │
│       │                          ▼                        │             │
│       │                     ┌─────────┐                   │             │
│       │                     │ paused  │                   │             │
│       │                     └────┬────┘                   │             │
│       │                          │ enabled=True           │             │
│       │                          ▼                        │             │
│       │                     ┌─────────┐                   │             │
│       │                     │ waiting │◀──────────────────┤             │
│       │                     └─────────┘    下次循环       │             │
│       │                                   (间隔触发器)    │             │
│       │                                                   │             │
│       │  ┌──────────────────────┬────────────────────────┘             │
│       │  │                      │                                       │
│       │  ▼                      ▼                                       │
│       │ ┌──────────┐       ┌─────────┐                                  │
│       │ │ completed│       │  error  │                                  │
│       │ │  (成功)  │       │  (失败) │                                  │
│       │ └──────────┘       └─────────┘                                  │
│       │       │                 │                                       │
│       └───────┴─────────────────┘                                       │
│           重新启用/手动执行                                              │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 📏 状态规则表

| 当前状态 | 事件 | 新状态 | 说明 |
|----------|------|--------|------|
| idle | 启用任务 | waiting | 开始等待触发 |
| waiting | 触发条件满足 | running | 开始执行 |
| waiting | 禁用任务 | paused | 暂停等待 |
| running | 执行成功 | completed/waiting | 单次完成/循环继续 |
| running | 执行失败 | error | 记录错误 |
| paused | 启用任务 | waiting | 恢复等待 |
| error | 重试/手动执行 | running | 重新执行 |
| completed | 重新启用 | waiting | 再次等待触发 |

---

## 数据流程

### 💾 配置文件存储

```
项目目录/
├── configs/
│   ├── tool_groups.json        # 工具分组配置
│   ├── local_settings.json     # 本地设置（DCC路径等）
│   └── automation_tasks.json   # 自动化任务配置 ⚠️ 自动生成
│
└── 用户文档目录 (~/Documents/DCC_Tool_Manager/)
    ├── local_scripts/          # 本地脚本（不纳入Git）
    │   ├── maya/
    │   ├── max/
    │   ├── blender/
    │   ├── ue/
    │   └── other/
    └── config/
        ├── local_settings.json
        └── user_preferences.json
```

### 🔄 任务数据流

```
┌─────────────────┐
│  用户创建任务   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐      ┌─────────────────┐
│ AutomationDialog│─────▶│ TriggerConfig   │
│  (任务配置UI)   │      │  Widget         │
└────────┬────────┘      └────────┬────────┘
         │                        │
         │  任务数据              │ 触发器配置
         ▼                        ▼
┌─────────────────────────────────────────────┐
│          AutomationTask 数据结构            │
│  {                                          │
│    "task_id": "uuid",                       │
│    "name": "任务名称",                       │
│    "tool_path": "plugins/xxx/",             │
│    "trigger_type": "interval",              │
│    "trigger_config": {...},                 │
│    "enabled": true,                         │
│    "status": "waiting"                      │
│  }                                          │
└─────────────────────────────────────────────┘
         │
         │ 保存到文件
         ▼
┌─────────────────────────────────────────────┐
│        automation_tasks.json                │
└─────────────────────────────────────────────┘
         │
         │ 加载到内存
         ▼
┌─────────────────────────────────────────────┐
│       AutomationManager.tasks               │
│       (运行时任务字典)                       │
└─────────────────────────────────────────────┘
```

---

## 附录：快速参考

### 🚀 常用操作

| 操作 | 入口 |
|------|------|
| 启动管理器 | 双击 `启动工具管理器.bat` |
| 刷新工具列表 | 点击"刷新工具列表"按钮 |
| 连接DCC软件 | 选择DCC → 点击"连接" |
| 管理自动化任务 | 菜单 → 自动化 → 任务管理 |
| Git更新 | 菜单 → Git管理 → 拉取更新 |
| 管理分组 | 菜单 → 设置 → 管理分组 |

### ⌨️ 快捷键

| 快捷键 | 功能 |
|--------|------|
| `F5` | 刷新工具列表 |
| `Win + .` | 打开Emoji选择器（分组图标输入） |

### 📞 端口配置

| 软件 | 端口 | 协议 |
|------|------|------|
| Maya | 7001 | TCP Socket |
| 3ds Max | 7002 | TCP Socket |
| Blender | 7003 | TCP Socket |
| Unreal Engine | 30010 | HTTP REST |

---

*文档版本: 2.0.0 | 最后更新: 2026-02-16*
